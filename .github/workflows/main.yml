name: Build EXE and Create Release

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest  # Chạy trên Windows runner

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build with PyInstaller
        run: pyinstaller --onefile dieukhieniq200.py

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v2  # Dùng phiên bản cũ hơn (v2)
        with:
          name: myapp-exe
          path: dist/dieukhieniq200.exe

      - name: Create Release
        run: |
          gh auth login --with-token ${{ secrets.PAT_TOKEN }}  # Đăng nhập bằng Personal Access Token (PAT)
          gh release create v1.${{ github.run_number }} dist/dieukhieniq200.exe
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
